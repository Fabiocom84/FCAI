// js/script.js

document.addEventListener('DOMContentLoaded', () => {
    // Questo è il posto per la logica generale del tuo sito,
    // che non è specifica di un singolo modale.
    // Tutte le funzionalità di aggiornamento della Knowledge Base AI
    // associate a 'updateAIDbBtn' sono gestite in 'chat-modal.js'.

    // Qui puoi aggiungere altre funzioni o inizializzazioni che si applicano all'intera pagina,
    // come l'aggiornamento degli "Ultimi Inserimenti" o altre utility globali.
});